{% extends 'template.html.twig' %}

{% block title %}Liste des utilisateurs{% endblock %}

{% block body %}
<table class="table">
  <thead>
    <tr>
        <th scope="col">Prenom</th>
        <th scope="col">Nom</th>
        <th scope="col">Email</th>
        <th scope="col">id</th>
        <th scope="col">Détails</th>


    </tr>
  </thead>
        <tbody>
            {% for personne in personnes %}
                <tr>
                    <td>{{ personne.firstname }}</td>
                    <td>{{ personne.name }}</td>
                    <td>{{ personne.email }}</td>
                    <td>{{ personne.id }}</td>
                    <td><a class="btn btn-primary" href="{{ path('dashboard.detail', {'id': personne.id}) }}" role="button">Afficher</a></td>
                </tr>
            {% endfor %}
        </tbody>
    </table>
{% endblock %}

{% block PageTitle %}
Liste des utilisateurs
{% endblock %}